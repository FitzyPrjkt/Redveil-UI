# WPOC-9651D6: Potential Open Redirect via 'to' Parameter

**Severity:** LOW  
**Confidence:** LOW  
**Status:** LIKELY  

**Affected endpoint:** `GET http://127.0.0.1/redirect`
**Parameter:** `to`
**Input used:** `https://example.com`

## Summary

An endpoint accepts a redirect-target parameter (`next`, `url`, `redirect`, `return`, `goto`, etc.) without server-side validation. If exploited, an attacker can craft a link on the trusted domain that redirects victims to a phishing or malware site — bypassing URL filters and user trust signals.

## Technical explanation

Open redirects exploit authentication flows that use a `returnTo` or `next` parameter after login. The application redirects to whatever URL the user supplies, with no validation. Because the initial URL is on the trusted domain, link previewers and email scanners see only the trusted host. The victim clicks, is briefly redirected to the trusted domain, then sent to the attacker's site. Open redirects are routinely used in OAuth flows to hijack authorization codes, in phishing campaigns to make links look legitimate, and in tab-nabbing scenarios.

## Attack scenario

1. Attacker crafts `https://target.example/login?next=https://evil.example/fake-login`
2. Attacker distributes the link: 'Click here to verify your account on target.example'
3. Victim clicks, lands on target.example's real login page, enters credentials, gets redirected to evil.example
4. evil.example renders a target.example lookalike saying 'Session expired, please re-enter your password'
5. Victim enters credentials — captured by attacker
6. Attacker uses credentials to log in to the real target.example — account takeover

## Steps to reproduce


## Impact

Phishing leverage: any link on the trusted domain can land the victim on an attacker-controlled page. OAuth flow abuse: if the OAuth `redirect_uri` is not validated, the attacker captures the authorization code. Tab-nabbing: a link opened in a new tab can rewrite the original tab via window.opener. The combination with OAuth is the most damaging — full account takeover via stolen authorization code.

## Evidence

_1 evidence record(s). See `evidence/WPOC-9651D6-*.txt` in the report directory._

## Remediation

1. Validate redirect targets against an allowlist of internal paths (e.g. only redirect to paths starting with `/` on your own domain).
2. Use signed/encrypted tokens for redirect targets so the URL itself cannot be modified by an attacker.
3. If external redirects are needed, present an interstitial page warning the user they are leaving the trusted domain.
4. For OAuth, validate `redirect_uri` strictly — only allow pre-registered URIs; reject wildcards and partial matches.

## Code examples

### flask

```
from urllib.parse import urlparse
ALLOWED_HOSTS = {'target.example.com'}
@app.route('/login')
def login():
    next_url = request.args.get('next', '/')
    p = urlparse(next_url)
    if p.scheme or p.netloc:  # absolute URL — reject
        return 'invalid redirect', 400
    if not next_url.startswith('/'):
        next_url = '/' + next_url
    return redirect(next_url)
```

### django

```
from django.utils.http import url_has_allowed_host_and_scheme
def safe_redirect(request):
    nxt = request.GET.get('next', '/')
    if url_has_allowed_host_and_scheme(nxt, allowed_hosts={request.get_host()}, require_https=request.is_secure()):
        return redirect(nxt)
    return redirect('/')
```

### express

```
function safeRedirect(req, res, next) {
  const nxt = req.query.next || '/';
  if (/^\/[\w/-]*$/.test(nxt)) {
    return res.redirect(nxt);
  }
  return res.redirect('/');
}
```

### oauth

```
# OAuth: validate redirect_uri strictly
ALLOWED_REDIRECTS = {'https://app.example.com/callback'}
if redirect_uri not in ALLOWED_REDIRECTS:
    return error('invalid redirect_uri')
```

### interstitial

```
# If external redirects must be allowed, use an interstitial:
<a href="/leave?u=https://external.example">Leave site</a>
# /leave renders: 'You are about to visit https://external.example'
```

## References

- CWE-601 (https://cwe.mitre.org/data/definitions/601.html)
- OWASP A01:2021

---
*Discovered: 2026-08-31T22:06:59.085895+00:00*  
*Tool: redveil v0.1.0*